version: "3.8"

services:
  docs-calidad:
    build: .
    container_name: docs-calidad
    ports:
      - "3001:3001"
    volumes:
      - ./estructura.json:/app/estructura.json
    restart: unless-stopped

